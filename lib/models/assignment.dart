class Assignment {
  String due;
  String name;
  num? score;
  num points;
  String category;
  bool graded;

  Assignment(this.due, this.name, this.score, this.points, this.category, this.graded);
}